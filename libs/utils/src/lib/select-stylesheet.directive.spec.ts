import { SelectStylesheetDirective } from './select-stylesheet.directive';

describe('SelectStylesheetDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectStylesheetDirective();
    expect(directive).toBeTruthy();
  });
});
